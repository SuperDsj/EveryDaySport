Bin4 = list_to_binary([Bin,[9,Bin2],4,6,7,8,9|Bin3]).
Bin=<<1,2,3,4,5>>.
Bin3= <<6>>
Bin2 = <<4,5>>.
split_binary(Bin4,5).

B=term_to_binary({binaries,"are",useful}).
binary_to_term(B).
%% 位语法中，需要理解的是最终定义的长度，是否满足二进制型，不满足则为位串，定义位语法的时候，需要先定义对应的变量，然后创建位语法，方便二进制型数据的传输。
%% 简短的代码实现打包和解包二进制数据结构，但是需要了解其结构特征，不然不能正确的解包数据。如果不是二进制型，是位串形式，也可以同样适用模式匹配获取数据内容。
Red=2.
Green=61.
Blue=20.
Meu = <<Red:5,Green:6,Blue:5>>.

{<<16#123456789:32/big>>,<<16#123456789:32/little>>,<<16#123456789:32/native>>,<<16#123456789:32>>}.
%% MPEG  mp3语音格式头

%% 位语法，主要适用于二进制和字节的处理，在文件存储，套接字使用的最为广泛

beam_lib:chunks("attrs.beam",[attributes]).
attrs:module_info(compile).
可以用来查看模块属性。
